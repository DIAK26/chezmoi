# Path to your Oh My Zsh installation
export ZSH="$HOME/.oh-my-zsh"

ZSH_THEME=""

plugins=(git)

source $ZSH/oh-my-zsh.sh

# -------------------------------------------
# PATH
# -------------------------------------------
export PATH="$HOME/.local/kitty.app/bin:$PATH"
export PATH="$HOME/.local/bin:$PATH"

# -------------------------------------------
# SHELL ENHANCEMENTS
# -------------------------------------------
source /usr/share/zsh-autosuggestions/zsh-autosuggestions.zsh
source /usr/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh

# -------------------------------------------
# FZF
# -------------------------------------------
source /usr/share/doc/fzf/examples/key-bindings.zsh
source /usr/share/doc/fzf/examples/completion.zsh
# -------------------------------------------
# ALIASES
# -------------------------------------------
alias ls="lsd"
alias ll="lsd -l"
alias la="lsd -la"
alias lt="lsd --tree"
alias ld="lsd --tree --depth 3"
alias bat="batcat"
# Datei schnell suchen und öffnen
alias f='nano $(find ~ -type f | fzf)'

# Zu Verzeichnis springen
alias cd_find='cd $(find ~ -type d | fzf)'

# Datei kopieren
alias cp_find='cp $(find ~ -type f | fzf -m) '

# NEUE ALIASES HINZUFÜGEN ↓
alias gs="git status"
alias ga="git add -A"
alias gc="git commit -m"
alias gp="git push"
alias gpl="git pull"
alias gd="git diff"
alias gco="git checkout"

# --------------------------------------------
# PROMPT (muss am Ende stehen)
# --------------------------------------------
eval "$(starship init zsh)"
alias ssh="kitten ssh"
